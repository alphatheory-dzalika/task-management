This is the DepartmentSnapshot, but using External objects.

What are the External objects?

They are extensions of the internal objects, but including the natural keys or related elements for every related data element expressed as a Surrogate key.

The reason for the external model?

Because to incorporate external data into the Alpha Theory systems, we first need to translate the incoming data 